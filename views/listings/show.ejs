<%- layout('layouts/boilerplate') %>


<div class="row mt-4">

    <!-- Listing Card -->
    <div class="col-lg-8 offset-lg-2 ">

        <div class="show-listing-card listing-card">

            <div class="show-img-container">
                <img
                    src="<%= listing.image.url %>"
                    alt="listing image"
                    class="listing-card-img-top show-img"
                >
            </div>

            <div class="listing-card-body show-body">

                <h2 class="show-title">
                    <%= listing.title %>
                </h2>

               <p class="hosted-by">
    <i class="fa-solid fa-user"></i>
    Hosted by
    <strong><%= listing.owner.username %></strong>
</p>

                <p class="show-description">
                    <%= listing.description %>
                </p>

                <hr>

              <div class="listing-details">

    <div class="row g-3">

        <div class="col-md-6">
            <p class="detail-item">
                <i class="fa-solid fa-location-dot detail-icon"></i>
                <strong>Location</strong><br>
                <span><%= listing.location %></span>
            </p>
        </div>

        <div class="col-md-6">
            <p class="detail-item">
                <i class="fa-solid fa-earth-asia detail-icon"></i>
                <strong>Country</strong><br>
                <span><%= listing.country %></span>
            </p>
        </div>

        <div class="col-md-6">
            <p class="detail-item">
                <i class="fa-solid fa-tag detail-icon"></i>
                <strong>Category</strong><br>

                <span class="category-badge">

                    <% if(listing.category==="Mountain"){ %>🏔<% } %>
                    <% if(listing.category==="Beach"){ %>🏖<% } %>
                    <% if(listing.category==="Forest"){ %>🌲<% } %>
                    <% if(listing.category==="Lakefront"){ %>🌊<% } %>
                    <% if(listing.category==="Camping"){ %>🏕<% } %>
                    <% if(listing.category==="City"){ %>🏙<% } %>
                    <% if(listing.category==="Heritage"){ %>🏰<% } %>
                    <% if(listing.category==="Nature"){ %>🌿<% } %>
                    <% if(listing.category==="Safari"){ %>🦁<% } %>
                    <% if(listing.category==="Luxury"){ %>💎<% } %>

                    <%= listing.category %>

                </span>
            </p>
        </div>

       <div class="price-box">

    <span class="price-value">
        ₹ <%= listing.price.toLocaleString("en-IN") %>
    </span>

    <span class="price-unit">
        / night
    </span>

</div>

    </div>

</div>
                <% if(currUser&&currUser._id.equals(listing.owner._id)) {%>
                <div class="show-btns">

                    <a
                        href="/listings/<%= listing._id %>/edit"
                        class="btn btn-dark">
                        Edit
                    </a>

                    <form
                        action="/listings/<%= listing._id %>?_method=DELETE"
                        method="POST"
                        class="d-inline">

                        <button
                            type="submit"
                            class="btn btn-danger">
                            Delete
                        </button>

                    </form>

                </div>
                <% } %>
               
            </div>
             
        </div>

       

    </div>

       <div class="col-lg-8 offset-lg-2  mb-4">
    <hr>
    <h4>Where you'll be </h4>
    <p class="text-muted mb-3">
        Check out the location of this property on the map below:</p>
        
   <div class="card shadow-sm">
    <div class="card-body p-2">
        <div id="map"></div>
    </div>
</div>

</div>

    <!-- Review Form -->
    <% if(currUser) { %>
        
    <div class="col-lg-8 offset-lg-2  mb-3">
        <hr>
        <h4>Leave a Review</h4>

        <form
            action="/listings/<%= listing._id %>/reviews"
            method="POST"
            class="needs-validation"
            novalidate>

            <div class="mb-3 mt-3">
                <label for="rating" class="form-label">
                    Ratings:
                </label>

                   <fieldset class="starability-slot">
 

    <input
        type="radio"
        id="no-rate"
        class="input-no-rate"
        name="review[rating]"
        value="1"
        checked
       
    />

    <input
        type="radio"
        id="first-rate1"
        name="review[rating]"
        value="1"
    />
    <label for="first-rate1" title="Terrible">
        1 star
    </label>

    <input
        type="radio"
        id="first-rate2"
        name="review[rating]"
        value="2"
    />
    <label for="first-rate2" title="Not good">
        2 stars
    </label>

    <input
        type="radio"
        id="first-rate3"
        name="review[rating]"
        value="3"
    />
    <label for="first-rate3" title="Average">
        3 stars
    </label>

    <input
        type="radio"
        id="first-rate4"
        name="review[rating]"
        value="4"
    />
    <label for="first-rate4" title="Very good">
        4 stars
    </label>

    <input
        type="radio"
        id="first-rate5"
        name="review[rating]"
        value="5"
    />
    <label for="first-rate5" title="Amazing">
        5 stars
    </label>
</fieldset>
                <!-- <input
                    type="range"
                    min="1"
                    max="5"
                    step="1"
                    id="rating"
                    name="review[rating]"
                    class="form-range"> -->
            </div>

            <div class="mb-3 mt-3">

                <label for="comment" class="form-label">
                    Comments:
                </label>

                <textarea
                    id="comment"
                    name="review[comment]"
                    rows="4"
                    class="form-control"
                    placeholder="Add a comment"
                    required></textarea>

                <div class="invalid-feedback">
                    Please submit some comments!
                </div>

            </div>

            <div class="show-btns mb-3">
                <button class="btn btn-dark">
                    Submit
                </button>
            
            </div>

        </form>
        

    </div>

    <% } %>


    <!-- Reviews Section -->
   <% if(listing.reviews.length > 0) { %>
     
    <div class="col-lg-8 offset-lg-2">
       <hr>
        

        <h4>All Reviews:</h4>

        <div class="row mt-4 mb-3">

            <% for(let review of listing.reviews){ %>

                <div class="card col-md-5 mt-3 ms-3 mb-4 review-card">

                    <div class="card-body">

                        <h5 class="card-title">
                            <b>@<%=review.author.username%></b>
                        </h5>

                    <p class="starability-result" data-rating="<%= review.rating %>">
    Rated: <%= review.rating %> stars
</p>

                        <p class="card-text">
                            <i>- <%= review.comment %></i>
                        </p>
                        <% if(currUser && currUser._id.equals(review.author._id)) { %>
                        <form
                            action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE"
                            method="POST">

                            <button
                                type="submit"
                                class="btn btn-danger mb-3">
                                Delete
                            </button>

                        </form>
                        <% } %>
                    </div>

                </div>

            <% } %>

        </div>

    </div>
    <% } %>


</div>

<script>
    const coordinates = <%- JSON.stringify(listing.geometry.coordinates) %>;
    const listingLocation = "<%= listing.location %>";
    const listingTitle = "<%= listing.title %>";
</script>

<script src="/js/map.js"></script>

